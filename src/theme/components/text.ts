import { defineStyleConfig } from "@chakra-ui/styled-system";

export default defineStyleConfig({
    baseStyle: {
        fontSize: "18px",
        fontWeight: "medium",
        lineHeight: "28px",
        color: "brand.gray.normal"
    }
})